[phases.setup]
aptPkgs = ["libpq-dev", "python3", "python3-pip"]

[phases.build]
cmds = [
  "docker-php-ext-install pdo pdo_pgsql pgsql",
  "pip3 install reportlab pypdf Pillow 'qrcode[pil]' --break-system-packages"
]

[start]
cmd = "frankenphp run --config /Caddyfile"
